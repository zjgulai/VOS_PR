# VOC 数据产品上线验收清单（收口版）

最后更新：2026-03-31

## 一、范围收口结论

- [x] 文档体系重构完成（`01`~`05` 主目录已建立并可按工作流阅读）
- [x] 采集难点诊断完成（登录墙/反爬/评论树等问题已形成标准化方案）
- [x] 工作流引擎完成（任务注册、状态管理、执行日志、登录墙记录）
- [x] 前台看板完成（数据浏览、问题雷达、竞品趋势）
- [x] 后台看板完成（任务队列、登录墙、批次复盘、执行闭环）
- [x] 周会交付链路完成（CSV/XLSX 导出 + 一键闭环执行 + 审计历史）

## 二、后端验收

### 1) 核心健康
- [x] `GET /health` 可用
- [x] 关键业务 API 均返回 200（见"接口抽样验收"）

### 2) VOC 查询能力
- [x] 国家/平台枚举接口可用
- [x] 摘要与问题雷达可用
- [x] 竞品趋势接口可用（时间窗口、国家、TopN 参数）

### 3) 任务运维能力
- [x] 任务列表支持多维筛选（status/country/platform/mode）
- [x] 支持单任务执行、批量 pending 执行、指定任务批量执行
- [x] 支持批量任务状态更新

### 4) 登录墙闭环能力
- [x] 登录墙记录支持筛选与单条更新
- [x] 登录墙记录支持批量更新
- [x] 登录墙记录支持回推任务（可选立即执行）

### 5) 周会闭环能力
- [x] 批次复盘导出（rows/trends CSV）
- [x] 周会闭环包导出（CSV）
- [x] 周会闭环包导出（XLSX，多 sheet）
- [x] 周会闭环执行接口可用（按 `manual_done` 自动回推/执行）
- [x] 闭环执行审计落库（`weekly_closure_runs`）
- [x] 闭环历史查询接口可用

## 三、前端验收

- [x] `/explore` 页面可访问，核心组件加载正常
- [x] `/admin` 页面可访问，关键管理组件加载正常
- [x] 批次复盘面板支持 CSV/XLSX 导出按钮
- [x] 登录墙面板支持筛选、勾选、批量更新、回推任务
- [x] 任务队列支持筛选、勾选、批量改状态、批量执行
- [x] 批次管理支持"执行周会闭环"与闭环历史展示

## 四、UI 重设计验收（2026-03-31 新增）

### 1) 设计系统
- [x] `globals.css` 定义完整 CSS custom properties（色板、字体、间距、阴影、圆角）
- [x] 主色 `#10b981` tech-green 贯穿品牌色、按钮、导航激活态
- [x] 页面背景 `#f5f8f7` → 白色卡片 → 灰白交替行 三层视觉层次
- [x] Inter 字体全局生效，字重 400/500/600/700 四级层次
- [x] 语义状态色（success/warning/error/info）及其 background 变体定义完整

### 2) 站点框架
- [x] 顶部导航栏 glassmorphism 毛玻璃效果（`backdrop-filter: blur`）
- [x] 导航激活态 pill badge（绿底白字）正确高亮当前页
- [x] 品牌 logo + 文字左对齐，导航链接右对齐
- [x] `page-shell` 容器统一 `max-width: 1200px` 居中布局

### 3) 页面级视觉回归
- [x] `/` 首页：hero 渐变区、4 张能力卡片、4 个统计指标
- [x] `/explore`：页面标题区、VOC 浏览器卡片、竞品趋势 + 问题雷达双栏
- [x] `/research`：页面标题区、tabs 导航、维表浏览器全宽卡片
- [x] `/admin`：四大分区（运营概览/任务执行/异常处理/系统健康）正确渲染

### 4) 组件级视觉一致性
- [x] 全部 14 个组件（含 CollectionStatus）已迁移至设计系统 CSS class
- [x] 卡片标题左侧绿色竖线标记（`.card-title::before`）
- [x] 筛选区 `.filter-bar` 统一灰底行布局
- [x] 表格 `.table-wrap` 统一样式（header 灰底、斑马条纹、hover 高亮）
- [x] 按钮 `.btn-primary` 绿底白字、圆角、hover 暗一档
- [x] 状态标签 `.badge` 四种语义色（success/warning/error/neutral）
- [x] 进度条 `.meter` + `.meter-fill` 统一样式
- [x] 告警区 `.alert-error` / `.alert-info` / `.alert-success` 统一样式
- [x] 无内联 `style=` 残留（已全部迁移至 CSS class）

### 5) 构建与兼容性
- [x] `npx next build` 零错误零警告
- [x] 浏览器截图四页均正确渲染（Chrome/Edge）

## 五、质量与稳定性验收

- [x] 前端构建通过（`npm run build`）
- [x] 后端关键接口 smoke test 通过
- [x] 最近改动文件无新增 lints 报错
- [x] 收口文档与 API README 已同步

## 六、暂缓项（2026-04-01 已补齐）

- [x] 负责人维度汇总 sheet（按 owner 的待处理数/完成率）
- [x] 闭环历史筛选与导出（按时间/模式）
- [x] 闭环历史失败重试快捷动作
- [x] 暗色模式切换（支持浅色/深色切换并记忆）
- [x] 响应式断点优化（平板/手机端布局适配）

## 七、上线前操作建议（执行顺序）

0. 先阅读运营手册：`docs/06_收口与验收/周会闭环操作手册_运营版.md`
1. 在 `voc-data-product/` 目录下运行 `.\start-dashboard.ps1` 启动前后端（或从仓库根目录运行 `scripts\start-voc-dashboard.ps1`，该脚本会自动代理到新版入口）
2. 检查 `/health` 与 `/admin` 页面可访问
3. 逐页目视检查 UI 是否与设计系统一致
4. 在后台执行一次 `weekly-closure-execute`（建议先 dry_run）
5. 导出一次 `weekly-closure-export-xlsx` 做周会演练
6. 确认 `weekly-closure-history` 出现新增审计记录

---

结论：本轮已完成从"功能可用"到"视觉专业化"的完整升级，设计系统覆盖色板/字体/组件/状态/布局五个层面，14 个组件全部迁移至统一样式体系，四页视觉回归均通过。
